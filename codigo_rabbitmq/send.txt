const celery = require('celery-node');

const worker = celery.createWorker(
    "amqp://admin:secret@localhost/myvhost",
    "redis://localhost:6379",
    "luuna_order_dev"
);
worker.register("new_order", (a) => {
    console.log(a)
});

worker.start();
